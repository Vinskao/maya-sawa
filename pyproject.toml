[tool.poetry]
name = "maya-sawa"
version = "0.1.0"
description = "A document Q&A system using LangChain and ChromaDB"
authors = ["Your Name <your.email@example.com>"]
packages = [{ include = "maya_sawa" }]

[tool.poetry.dependencies]
python = "^3.9"
langchain = "^0.3.25"
langchain-openai = "^0.2.0"
chromadb = "^0.4.24"
openai = "^1.22.0"
python-dotenv = "^1.0.0"
tiktoken = ">=0.7,<1"
numpy = ">=1.24.0,<2.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
